# column names used in dplyr verbs (non-standard evaluation), declared here so
# that R CMD check does not report them as undefined global variables
#' @importFrom utils globalVariables
NULL

globalVariables(c("variable"))
